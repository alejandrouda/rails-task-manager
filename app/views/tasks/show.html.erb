<h1><%= @task.title %></h1>

<p><%= @task.details %></p>

<p><%= link_to "Destroy #{@task.title}", task_path(@task), method: :delete, data: {confirm: "Are you sure you want to destroy #{@task.title}"}  %></p>